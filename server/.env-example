PORT= 
FRONTEND_URL= 
# Chain
SONIC_RPC_URL= 
MULTICALL3_ADDRESS= 
# Vaults (deployed on Sonic 146)
INDEXER_START_BLOCK= 
# Ponder DB writer (set your Supabase Postgres connection URL Session pooler)
DATABASE_URL= 

# DATABASE_SCHEMA=public
# Supabase API
SUPABASE_PROJECT_REF= 
SUPABASE_URL= 
SUPABASE_SERVICE_ROLE_KEY= 
# AI (optional; enables AI endpoints and auto-rebalancing logic)
ANTHROPIC_API_KEY= 
# Automated Rebalancing (optional)
ENABLE_AUTO_REBALANCING= 
# Wallet for on-chain rebalancing execution (must be authorized in vault)
STRATEGY_MANAGER_PRIVATE_KEY= 
# Admin API (protects rebalance + scheduler endpoints). Choose a strong secret
ADMIN_API_KEY= 
CHAIN_ID= 
VAULT_ADDRESSES= 
DEFAULT_VAULT_ID= 
# Indexer schema strategy
# For dev, use a stable schema to avoid reindex on restarts
INDEXER_DEV_SCHEMA= 
# For production starts, a unique schema is generated as: `${INDEXER_SCHEMA_PREFIX}_YYYYMMDDhhmmss`
INDEXER_SCHEMA_PREFIX= 
# Views schema is the stable read schema that always points to the latest deployment
INDEXER_VIEWS_SCHEMA= 
# Optional: if you run Ponder without our scripts, this can be used by ponder.config.ts
DATABASE_SCHEMA= 
# Set CORS origins (comma-separated) for frontend access; defaults to localhost and VEYRA
FRONTEND_URL= 
# Cloudflare Tunnel Token (for production deployment)
TUNNEL_TOKEN= 